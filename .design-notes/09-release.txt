<div>  { padding:18px 28px 40px; flex-direction:column; gap:16px }
  <div>  { align-items:baseline; gap:12px }
    <div>  { font-size:14px; font-weight:600 }
      · {{ checklistHeader }}
    <div>  { font-size:12px; color:var(--text2) }
      · Every row below happens in a console. No API performs it.
  <div>  { grid-template-columns:{{ checklistGrid }}; gap:14px; align-items:start }
    <sc-for>
      <div>  { min-width:0; border:.5px solid var(--sep); border-radius:9px; background:var(--raised); overflow:hidden }
        <div>  { padding:10px 14px; align-items:baseline; justify-content:space-between; gap:8px }
          <div>  { font-size:12.5px; font-weight:600 }
            · {{ c.name }}
          <div>  { color:var(--text2) }
            · {{ c.done }}
        <sc-for>
          <div>  { grid-template-columns:15px minmax(0,1fr) 60px; align-items:center; padding:10px 14px }
            <div>  { width:15px; height:16px; align-items:center; justify-content:center }
              <sc-if>
                <div>  { width:14px; height:14px; border-radius:3px; border:1px solid var(--sep); background:{{ r.checkBg }}; color:#fff; font-size:9px; align-items:center; justify-content:center | on={{ r.toggle }} }
                  · {{ r.checkMark }}
            <div>  { min-width:0; font-size:12px; line-height:1.35 }
              · {{ r.title }}
            <div>  { padding:2px 0; border-radius:5px; background:{{ r.bg }}; color:{{ r.fg }} }
              · {{ r.state }}
            <div>  { min-width:0; font-size:10.5px; color:var(--text2); line-height:1.4 }
              · {{ r.reason }}
            <div>  { font-size:10.5px; color:var(--accent) }
              · Open ↗
  <div>  { grid-template-columns:1fr 1fr; gap:14px }
    <sc-for>
      <div>  { min-width:0; border:.5px solid var(--sep); border-radius:9px; background:var(--raised); padding:12px 15px; align-items:center; gap:11px }
        <div>  { width:8px; height:8px; border-radius:{{ s.dotRadius }}; background:{{ s.dotColor }} }
        <div>  { min-width:0 }
          <div>  { font-size:12.5px; font-weight:500 }
            · {{ s.store }}
          <div>  { font-size:11px; color:var(--text2); overflow:hidden }
            · {{ s.detail }}
        <div>  { font-size:12px; font-weight:500; color:{{ s.stateColor }} }
          · {{ s.state }}
  <div>  { flex-direction:column; gap:16px }
    <div>  { align-items:baseline; gap:11px }
      <div>  { font-size:14px; font-weight:600 }
        · Send to review
      <div>  { font-size:12px; color:var(--text2) }
        · One button per store. These two are the only irreversible actions in this app.
    <div>  { grid-template-columns:1fr 1px 1fr; gap:0; align-items:stretch }
      <div>  { min-width:0; gap:12px }
        <div>  { font-size:12px; color:var(--text2); line-height:1.55 }
          · App Store · version 3.2.0, build 412
          <br>
          · Release after approval, phased over 7 days.
        <button>  { font-size:13.5px; font-weight:600; padding:13px 18px; border-radius:9px; border:1px solid {{ appleBtnBorder }}; background:{{ appleBtnBg }}; color:{{ appleBtnFg }}; line-height:1.3; overflow:hidden | on={{ releaseApple }} }
          · {{ appleBtnLabel }}
        <div>  { font-size:11.5px; color:{{ appleHintColor }}; line-height:1.55 }
          · {{ appleHint }}
      <div>  { background:var(--sep) }
      <div>  { min-width:0; gap:12px }
        <div>  { font-size:12px; color:var(--text2); line-height:1.55 }
          · Google Play · version 3.2.0, version code 412
          <br>
          · Production track, completed rollout.
        <button>  { font-size:13.5px; font-weight:600; padding:13px 18px; border-radius:9px; border:1px solid {{ googleBtnBorder }}; background:{{ googleBtnBg }}; color:{{ googleBtnFg }}; line-height:1.3; overflow:hidden | on={{ releaseGoogle }} }
          · {{ googleBtnLabel }}
        <div>  { font-size:11.5px; color:{{ googleHintColor }}; line-height:1.55 }
          · {{ googleHint }}
