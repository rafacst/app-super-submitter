· <
· 5; V.obIsPromise = ob === 5; V.obNumber = Math.min(ob + 1, 5); V.obTitle = (steps[ob] || steps[4]).title; V.obPoints = (steps[ob] || steps[4]).points.map(t => ({ t })); for (let i = 1; i
· <
· = 5; i++) V['ob' + i] = ob === i - 1; V.obCanBack = ob > 0; V.obBack = () => this.set('obStep', Math.max(0, ob - 1)); V.obNext = () => { if (ob >= 5) this.setState({ onboarding: false, obStep: 0 }); else this.set('obStep', ob + 1); }; V.obNextLabel = ob === 4 ? 'One last thing' : ob === 5 ? 'Start' : 'Next'; V.obDots = Array.from({ length: 6 }, (_, i) => ({ go: () => this.set('obStep', i), w: i === ob ? 18 : 6, bg: i === ob ? 'var(--accent)' : 'var(--sep)' })); V.obPkgRows = [ { k: 'Bundle id', v: 'com.fastbillsplit.app' }, { k: 'Version', v: '3.2.0' }, { k: 'Build', v: '412' }, { k: 'Languages', v: 'en-US, pt-BR' }, { k: 'Minimum OS', v: 'iOS 17.0' } ]; // ---- app switcher ---- const appDefs = [ { name: 'Fast Bill Split', initials: 'FB', apple: 'y', google: 'y', summary: '3.2.0 · 24 changes wait' }, { name: 'Tide Timer', initials: 'TT', apple: 'g', google: 'g', summary: '1.8.1 · both stores match' }, { name: 'Receipt Vault', initials: 'RV', apple: 'r', google: 'g', summary: '2.0.0 · 1 error blocks the plan' } ]; const mark = { g: '✓', y: '!', r: '✕' }; const clr = { g: 'var(--green)', y: 'var(--yellow)', r: 'var(--red)' }; const bgc = { g: 'var(--greenBg)', y: 'var(--yellowBg)', r: 'var(--redBg)' }; V.apps = appDefs.map((a, i) => ({ name: a.name, initials: a.initials, summary: a.summary, select: () => this.setState({ appIdx: i, switcherOpen: false }), bg: i === st.appIdx ? 'var(--sep2)' : 'transparent', menuBg: i === st.appIdx ? 'var(--sunken)' : 'transparent', tick: i === st.appIdx ? '✓' : '', nameColor: 'var(--text)', appleMark: mark[a.apple], googleMark: mark[a.google], appleColor: clr[a.apple], googleColor: clr[a.google], appleBg: bgc[a.apple], googleBg: bgc[a.google] })); V.currentApp = V.apps[st.appIdx] || V.apps[0]; V.switcherOpen = !!st.switcherOpen; V.switcherChipBg = st.switcherOpen ? 'var(--sunken)' : 'var(--field)'; V.toggleSwitcher = () => this.set('switcherOpen', !st.switcherOpen); // ---- tabs ---- const icon = (paths) => React.createElement('svg', { width: 15, height: 15, viewBox: '0 0 16 16', fill: 'none', stroke: 'currentColor', strokeWidth: 1.35, strokeLinecap: 'round', strokeLinejoin: 'round' }, paths); const R = (p) => React.createElement('rect', p); const L = (p) => React.createElement('line', p); const C = (p) => React.createElement('circle', p); const P = (p) => React.createElement('path', p); const icons = [ icon([R({ key: 1, x: 1.5, y: 4, width: 5.5, height: 8, rx: 1.3 }), R({ key: 2, x: 9, y: 4, width: 5.5, height: 8, rx: 1.3 })]), icon([R({ key: 1, x: 2, y: 3.5, width: 12, height: 9, rx: 1.5 }), L({ key: 2, x1: 2, y1: 6.6, x2: 14, y2: 6.6 })]), icon([L({ key: 1, x1: 2.5, y1: 4.5, x2: 13.5, y2: 4.5 }), L({ key: 2, x1: 2.5, y1: 8, x2: 13.5, y2: 8 }), L({ key: 3, x1: 2.5, y1: 11.5, x2: 9.5, y2: 11.5 })]), icon([R({ key: 1, x: 2, y: 3, width: 12, height: 10, rx: 1.5 }), C({ key: 2, cx: 5.6, cy: 6.4, r: 1.1 }), P({ key: 3, d: 'M2.6 12 6.6 8.2l3 2.6 2.2-1.8 1.6 1.4' })]), icon([C({ key: 1, cx: 8, cy: 8, r: 5.6 }), L({ key: 2, x1: 8, y1: 4.6, x2: 8, y2: 11.4 }), P({ key: 3, d: 'M9.9 6.2H7.1a1.4 1.4 0 0 0 0 2.8h1.8a1.4 1.4 0 0 1 0 2.8H6.1' })]), icon([R({ key: 1, x: 2.5, y: 2.5, width: 11, height: 11, rx: 1.5 }), P({ key: 2, d: 'M5.3 8.2 7.2 10l3.6-4' })]), icon([R({ key: 1, x: 2, y: 3, width: 5, height: 10, rx: 1 }), R({ key: 2, x: 9, y: 3, width: 5, height: 10, rx: 1 })]), icon([C({ key: 1, cx: 8, cy: 8, r: 5.6 }), P({ key: 2, d: 'M8 4.8v6.4M5.4 8.6 8 11.2l2.6-2.6' })]), icon([P({ key: 1, d: 'M4 2.8 13 8l-9 5.2z' })]) ]; const tabNames = ['Stores', 'Build', 'Details', 'Media', 'Money', 'Review info', 'Plan', 'Submit', 'Release']; const subtitles = [ 'Choose the stores. Connect each one.', 'Choose what to submit.', 'The listing text, per language.', 'The screenshots and the videos.', 'The price, the availability, and the purchases.', 'Everything the reviewer needs.', 'Every change, before any write.', 'The run. It writes drafts.', 'The last check, then the two irreversible buttons.' ]; const kwOK = st.keywordsFixed; const badges = { 3: (st.applied || kwOK) ? 0 : 1, 6: st.applied ? 0 : 2, 7: st.applied ? 0 : (kwOK ? 2 : 3) }; const badgeErr = { 3: true, 7: true }; V.tabs = tabNames.map((n, i) => { const num = i + 1, sel = st.tab === num, b = badges[num] || 0; return { name: n, icon: icons[i], go: () => this.setState({ tab: num, menuBar: false }), bg: sel ? 'var(--accent)' : 'transparent', fg: sel ? 'var(--accentText)' : 'var(--text)', weight: sel ? 600 : 400, badge: b || null, badgeBg: sel ? 'rgba(255,255,255,.25)' : (badgeErr[num] ? 'var(--redBg)' : 'var(--yellowBg)'), badgeFg: sel ? '#fff' : (badgeErr[num] ? 'var(--red)' : 'var(--yellow)'), gapBefore: (num === 7 || num === 9) ? 6 : 0, ruleBefore: (num === 7 || num === 9) ? '.5px solid var(--sep)' : 'none', segRuleW: (num === 7 || num === 9) ? 1 : 0, segFlex: n.length + 4, segWeight: sel ? 600 : 500, segBg: sel ? 'var(--field)' : 'transparent', segFg: sel ? 'var(--text)' : 'var(--text2)', segShadow: sel ? '0 1px 2px rgba(0,0,0,.16)' : 'none' }; }); V.navSidebar = (st.nav || 'sidebar') === 'sidebar'; V.navTop = (st.nav || 'sidebar') === 'topbar'; V.setNavSidebar = () => this.set('nav', 'sidebar'); V.setNavTop = () => this.set('nav', 'topbar'); V.navSidebarBg = V.navSidebar ? 'var(--accent)' : 'transparent'; V.navSidebarFg = V.navSidebar ? '#fff' : 'var(--text)'; V.navTopBg = V.navTop ? 'var(--accent)' : 'transparent'; V.navTopFg = V.navTop ? '#fff' : 'var(--text)'; V.tabTitle = tabNames[st.tab - 1]; V.tabSubtitle = subtitles[st.tab - 1]; for (let i = 1; i
· <
· = 9; i++) V['isTab' + i] = st.tab === i; V.goTab3 = () => this.set('tab', 3); V.goTab7 = () => this.set('tab', 7); V.goTab9 = () => this.set('tab', 9); V.showLocalePicker = st.tab === 3 || st.tab === 4; V.setEn = () => this.set('locale', 'en-US'); V.setPt = () => this.set('locale', 'pt-BR'); V.enBg = st.locale === 'en-US' ? 'var(--accent)' : 'transparent'; V.enFg = st.locale === 'en-US' ? '#fff' : 'var(--text)'; V.ptBg = st.locale === 'pt-BR' ? 'var(--accent)' : 'transparent'; V.ptFg = st.locale === 'pt-BR' ? '#fff' : 'var(--text)'; V.showPreview = size !== 'min'; V.keywordsFixed = st.keywordsFixed; V.fixKeywords = () => this.set('keywordsFixed', true); V.kwText = kwOK ? 'bill,split,tip,receipt,restaurant,dinner,share,check,tab,friends' : 'bill,split,tip,receipt,restaurant,dinner,share,check,tab,friends,group,payment'; V.kwCount = kwOK ? '95 / 100' : '104 / 100'; V.kwCountColor = kwOK ? 'var(--text2)' : 'var(--red)'; V.kwCountWeight = kwOK ? 400 : 600; V.kwBorder = kwOK ? '.5px solid var(--sep)' : '1px solid var(--red)'; V.kwOver = !kwOK; V.kwPreviewLine = kwOK ? 'Keywords: 95 of 100 characters.' : 'Keywords will not be written. 104 of 100 characters.'; V.kwPreviewColor = kwOK ? 'var(--text2)' : 'var(--red)'; // tab 1 V.appleGuide = st.appleGuide; V.googleGuide = st.googleGuide; V.appleGuideArrow = st.appleGuide ? '▼' : '▶'; V.googleGuideArrow = st.googleGuide ? '▼' : '▶'; V.toggleAppleGuide = () => this.set('appleGuide', !st.appleGuide); V.toggleGoogleGuide = () => this.set('googleGuide', !st.googleGuide); // tab 2 V.buildRead = st.buildRead; V.needsBuild = !st.buildRead; V.readBuild = () => this.set('buildRead', true); V.iosFileLine = st.buildRead ? 'FastBillSplit.ipa · 118.4 MB' : 'iOS or macOS package'; V.aabFileLine = st.buildRead ? 'app-release.aab · 42.1 MB' : 'Android package'; const mono = 'var(--mono)'; V.packages = [ { title: 'iOS · FastBillSplit.ipa', file: 'build/FastBillSplit.ipa', rows: [ { k: 'Bundle id', v: 'com.fastbillsplit.app', font: mono }, { k: 'Version name', v: '3.2.0', font: mono }, { k: 'Build number', v: '412', font: mono }, { k: 'App name', v: 'Fast Bill Split', font: 'inherit' }, { k: 'Languages', v: 'en-US, pt-BR', font: mono }, { k: 'Minimum OS', v: 'iOS 17.0', font: 'inherit' }, { k: 'Devices', v: 'iPhone, iPad', font: 'inherit' }, { k: 'Encryption', v: 'No non-exempt encryption', font: 'inherit' }, { k: 'Privacy', v: '3 permissions found', font: 'inherit' } ] }, { title: 'Android · app-release.aab', file: 'build/app-release.aab', rows: [ { k: 'Package name', v: 'com.fastbillsplit.app', font: mono }, { k: 'Version name', v: '3.2.0-rc4', font: mono }, { k: 'Version code', v: '412', font: mono }, { k: 'App label', v: 'Fast Bill Split', font: 'inherit' }, { k: 'Languages', v: 'en-US, pt-BR', font: mono }, { k: 'Minimum SDK', v: '26 · Android 8.0', font: 'inherit' }, { k: 'Devices', v: 'Phone, tablet', font: 'inherit' }, { k: 'Encryption', v: 'Not applicable', font: 'inherit' }, { k: 'Privacy', v: '4 permissions found', font: 'inherit' } ] } ]; // tab 4 const shots = (n, w, h, bucket, stores) => Array.from({ length: n }, () => ({ w, h, bucket, stores })); V.mediaGroups = [ { name: 'Phone', count: '7 of 10', note: 'iPhone 6.7 inch and Pixel', dropW: 88, dropH: 156, shots: [ ...shots(4, 88, 156, 'iPhone 6.7 inch', 'App Store · Google Play'), ...shots(3, 88, 156, 'Pixel 6 · 1080 × 1920', 'Google Play') ] }, { name: 'Tablet 10 inch', count: '2 of 10', note: 'iPad Pro 12.9 inch', dropW: 156, dropH: 112, shots: shots(2, 156, 112, 'iPad Pro 12.9 inch', 'App Store · Google Play') }, { name: 'Desktop', count: '2 of 10', note: 'Mac App Store only', dropW: 176, dropH: 110, shots: shots(2, 176, 110, 'Mac 2880 × 1800', 'App Store') } ]; // tab 5 V.providers = [ { key: 'none', name: 'None', line: 'No provider. The stores hold the catalog and nothing mirrors it.' }, { key: 'revenuecat', name: 'RevenueCat', line: 'One app per store. This app creates two products for one id.' }, { key: 'adapty', name: 'Adapty', line: 'One app for both stores. One product carries both store ids.' } ].map(p => ({ ...p, select: () => this.set('provider', p.key), border: prov === p.key ? '1.5px solid var(--accent)' : '.5px solid var(--sep)', dot: prov === p.key ? 'var(--accent)' : 'var(--sep)', mark: prov === p.key ? '✓' : '' })); V.isRevenueCat = prov === 'revenuecat'; V.isAdapty = prov === 'adapty'; V.hasProvider = prov !== 'none'; V.entitlementLabel = prov === 'adapty' ? 'Access levels' : 'Entitlements'; V.offeringLabel = prov === 'adapty' ? 'Paywalls and placements' : 'Offerings'; V.scopes = ['apps:read', 'products:read_write', 'entitlements:read_write', 'offerings:read_write', 'packages:read_write'].map(k => ({ k })); V.plans = [ { id: 'com.fastbillsplit.app.premium.monthly', duration: '1 month', basePlan: 'monthly', name: 'Premium Monthly', price: '2.99' }, { id: 'com.fastbillsplit.app.premium.yearly', duration: '1 year', basePlan: 'annual', name: 'Premium Yearly', price: '24.99' } ]; // tab 6 const pill = (s) => s === 'Done' ? { bg: 'var(--greenBg)', fg: 'var(--green)' } : s === 'Needed' ? { bg: 'var(--yellowBg)', fg: 'var(--yellow)' } : s === 'Unknown' ? { bg: 'var(--sep2)', fg: 'var(--text2)' } : { bg: 'transparent', fg: 'var(--text3)' }; const row = (title, reason, state, action) => ({ title, reason, state, action, ...pill(state) }); V.reviewOpenRows = [ row('Age rating questionnaire', 'App Store · this app writes the answers. 12 questions, none answered.', 'Needed', 'Answer'), row('Content rating (IARC)', 'Google Play · console only. Open Policy, then App content, then Content rating.', 'Needed', 'Open ↗') ]; V.reviewRows = [ row('Review notes', 'App Store · this app writes them. Google Play keeps its own field in the console.', 'Done', 'Edit'), row('App categories', 'App Store · this app writes them. Finance, then Utilities.', 'Done', 'Edit'), row('Google Play category', 'Console only. The Android Publisher API writes no category.', 'Unknown', 'Open ↗'), row('Privacy policy URL', 'App Store · one per language. Google Play keeps it in the console.', 'Done', 'Edit'), row('App privacy, nutrition labels', 'App Store · console only. No API reads or writes them.', 'Unknown', 'Open ↗'), row('Data safety form', 'Google Play · this app writes it, pre-filled from the 4 permissions the build declares.', 'Done', 'Review'), row('Export compliance', 'From the build: no non-exempt encryption.', 'Done', 'Edit') ]; // tab 7 V.dryRun = st.dryRun; V.toggleDryRun = () => this.set('dryRun', !st.dryRun); V.dryRunTrack = st.dryRun ? 'var(--accent)' : 'var(--sep)'; V.dryRunJustify = st.dryRun ? 'flex-end' : 'flex-start'; V.planEmpty = st.applied; V.planHasChanges = !st.applied; V.matchRows = [ { system: 'App Store', line: 'Version 3.2.0, build 412. Listing, media, and purchases match.' }, { system: 'Google Play', line: 'Version code 412 in production. Listing, media, and purchases match.' }, { system: 'RevenueCat', line: '5 products, 2 entitlements, 1 offering. All match.' } ]; V.validations = [ ...(kwOK ? [] : [{ kind: 'Error', bg: 'var(--redBg)', fg: 'var(--red)', text: 'Keywords are 104 characters. The limit is 100.', where: 'App Store · Details · en-US · Keywords', button: 'Fix on Details', fix: () => this.set('tab', 3), ackable: false }]), { kind: 'Warning', bg: 'var(--yellowBg)', fg: 'var(--yellow)', text: 'The version name differs between the two packages.', where: 'Build · 3.2.0 and 3.2.0-rc4', button: 'Fix on Build', fix: () => this.set('tab', 2), ackable: true, ack: () => this.setState({ acked: { ...st.acked, w1: !st.acked.w1 } }), ackBg: st.acked.w1 ? 'var(--accent)' : 'transparent', ackMark: st.acked.w1 ? '✓' : '' }, { kind: 'Warning', bg: 'var(--yellowBg)', fg: 'var(--yellow)', text: 'pt-BR has screenshots for the App Store and none for Google Play.', where: 'Media · pt-BR · phone', button: 'Fix on Media', fix: () => this.set('tab', 4), ackable: true, ack: () => this.setState({ acked: { ...st.acked, w2: !st.acked.w2 } }), ackBg: st.acked.w2 ? 'var(--accent)' : 'transparent', ackMark: st.acked.w2 ? '✓' : '' } ]; const green = 'var(--green)', yellow = 'var(--yellow)', red = 'var(--red)'; const cols = [ { name: 'App Store', summary: '11 writes · 10 uploads', rows: [ { sign: '+', color: green, text: 'version 3.2.0 (PREPARE_FOR_SUBMISSION)' }, { sign: '~', color: yellow, text: 'en-US.whatsNew "Faster scanning and a new dark theme."' }, { sign: '~', color: yellow, text: 'en-US.promotionalText "Now with receipt scanning."' }, { sign: '+', color: green, text: 'pt-BR.description (1 402 chars)' }, { sign: '+', color: green, text: '9 screenshots · 24.2 MB' }, { sign: '+', color: green, text: 'build 412 · 118.4 MB' }, { sign: '~', color: yellow, text: 'price 4.99 USD → point 4.99 USD' }, { sign: '-', color: red, text: 'preview preview-6.5-en.mov' } ] }, { name: 'Google Play', summary: '9 writes · 5 uploads', rows: [ { sign: '~', color: yellow, text: 'en-US.fullDescription (318 chars)' }, { sign: '~', color: yellow, text: 'en-US.shortDescription (43 chars)' }, { sign: '+', color: green, text: 'pt-BR listing' }, { sign: '+', color: green, text: '4 phoneScreenshots · 9.8 MB' }, { sign: '+', color: green, text: 'bundle versionCode 412 · 42.1 MB' }, { sign: '~', color: yellow, text: 'track production release draft' }, { sign: '~', color: yellow, text: 'basePlan annual 24.99 USD' } ] }, { name: 'RevenueCat', summary: '4 writes', rows: [ { sign: '+', color: green, text: 'product com.fastbillsplit.app.pro (app_store)' }, { sign: '+', color: green, text: 'product com.fastbillsplit.app.pro (play_store)' }, { sign: '~', color: yellow, text: 'entitlement pro attach 2 products' }, { sign: '~', color: yellow, text: 'offering default package annual → position 2' } ] } ]; V.diffColumns = prov === 'none' ? cols.slice(0, 2) : cols; V.validationHeader = kwOK ? '2 warnings' : '1 error, 2 warnings'; V.validationNote = kwOK ? 'A warning needs one acknowledgement. Nothing blocks the apply.' : 'An error blocks the apply. A warning needs one acknowledgement.'; V.validationBorder = kwOK ? 'var(--yellow)' : 'var(--red)'; V.validationBg = kwOK ? 'var(--yellowBg)' : 'var(--redBg)'; V.validationHeaderColor = kwOK ? 'var(--yellow)' : 'var(--red)'; const blocked = !kwOK; V.applyLabel = st.dryRun ? 'Dry run' : 'Apply'; V.applyBg = blocked ? 'var(--sep2)' : 'var(--accent)'; V.applyFg = blocked ? 'var(--text3)' : 'var(--accentText)'; V.applyCursor = blocked ? 'not-allowed' : 'pointer'; V.applyNote = st.dryRun ? 'A dry run logs every request and sends none. Nothing reaches a store.' : blocked ? '1 error blocks the apply. Fix the keywords on Details, then this button writes a draft to each store.' : 'This writes 24 changes and 13 uploads. It ends with a draft in each store. It sends nothing to review.'; V.apply = () => { if (!blocked) this.apply(); }; // demo escape: allow apply from the error row button area V.applyForce = this.apply; // tab 8 V.runIdle = st.runIndex
· <
· 0; V.runStarted = st.runIndex >= 0; V.runDone = st.runDone; const items = this.items; const spinner = React.createElement('div', { style: { width: 12, height: 12, borderRadius: '50%', border: '1.6px solid var(--sep)', borderTopColor: 'var(--accent)', animation: 'spin .7s linear infinite' } }); V.runItems = items.map((it, i) => { const done = i
· <
· st.runIndex, active = i === st.runIndex; if (it.g) return { text: it.g, pad: '9px 15px', rowBg: 'var(--sunken)', size: '11px', weight: 600, color: 'var(--text2)', mark: '', meta: '' }; return { text: it.t, pad: '8px 15px', rowBg: 'transparent', size: '12.5px', weight: 400, color: done || active ? 'var(--text)' : 'var(--text3)', mark: done ? React.createElement('span', { style: { color: 'var(--green)', fontSize: 12 } }, '✓') : active ? spinner : React.createElement('span', { style: { color: 'var(--text3)', fontSize: 11 } }, '·'), meta: done || active ? it.m : '' }; }); const cur = items[st.runIndex]; V.uploading = !!(cur && cur.long); const pct = cur && cur.long ? Math.min(99, Math.round(st.ticks / this.cost(cur) * 100)) : 0; V.uploadPct = pct; const secs = Math.round(pct / 100 * 480); V.uploadClock = Math.floor(secs / 60) + ':' + String(secs % 60).padStart(2, '0'); V.logOpen = st.logOpen; V.logArrow = st.logOpen ? '▼' : '▶'; V.toggleLog = () => this.set('logOpen', !st.logOpen); V.logText = '14:02:11 POST /v1/appStoreVersions 201 412ms\n14:02:12 PATCH /v1/appStoreVersions/9f2c… 200 238ms\n14:02:13 POST /v1/appInfoLocalizations 201 301ms\n14:02:14 POST /v1/appScreenshots 201 190ms\n14:02:15 PUT …uploadOperations[1/6] 200 1.204s\n14:02:22 POST /v1/buildUploads 201 356ms'; // tab 9 const ck = st.checks; const clRow = (id, title, reason, state) => { const marked = !!ck[id]; const eff = state === 'Unknown' && marked ? 'Done' : state; return { title, reason, state: eff, ...pill(eff), checkable: state === 'Unknown', toggle: () => this.setState({ checks: { ...ck, [id]: !marked } }), checkBg: marked ? 'var(--accent)' : 'transparent', checkMark: marked ? '✓' : '' }; }; const iarcState = st.rechecked ? 'Done' : 'Needed'; const appleRows = [ clRow('a1', 'App privacy, nutrition labels', 'No API reads this. Open App privacy.', 'Unknown'), clRow('a2', 'App information and categories', 'Confirmed: Finance, then Utilities.', 'Done'), clRow('a3', 'Pricing and availability', 'Confirmed: 4.99 USD, 175 countries.', 'Done'), clRow('a4', 'The submitted version', 'Confirmed: 3.2.0, build 412.', 'Done'), clRow('a5', 'Agreements, tax, and banking', 'No API reads this. Open Business.', 'Unknown') ]; const googleRows = [ clRow('g1', 'Content rating (IARC)', 'Console: Policy, App content, Content rating.', iarcState), clRow('g2', 'Data safety', 'Confirmed: the form is complete.', 'Done'), clRow('g3', 'Country availability', 'Console: Production, Countries and regions.', 'Unknown'), clRow('g4', 'The release in the track', 'Confirmed: a draft release in production.', 'Done'), clRow('g5', 'App signing', 'Console: Setup, App signing.', 'Unknown'), clRow('g6', 'App access, the reviewer credentials', 'Console: Policy, App content, App access.', 'Unknown') ]; const rcRows = [ clRow('r1', 'The store credential upload', 'Dashboard only. Every provider needs it.', 'Unknown'), clRow('r2', 'The offering', 'Confirmed: 2 packages in default.', 'Done') ]; const cards = [ { name: 'App Store', rows: appleRows }, { name: 'Google Play', rows: googleRows } ]; if (prov !== 'none') cards.push({ name: prov === 'adapty' ? 'Adapty' : 'RevenueCat', rows: rcRows }); cards.forEach(c => { c.done = c.rows.filter(r => r.state === 'Done').length + ' of ' + c.rows.length; }); V.checklistCards = cards; V.checklistGrid = cards.length === 3 ? '1fr 1fr 1fr' : '1fr 1fr'; const all = cards.flatMap(c => c.rows); V.checklistHeader = all.filter(r => r.state === 'Done').length + ' of ' + all.length + ' steps are done'; V.recheck = () => this.set('rechecked', true); const appleState = st.appleReleased ? 'Waiting for review' : st.applied ? 'Draft, ready to release' : 'No draft yet'; const googleState = st.googleReleased ? 'Waiting for review' : st.applied ? 'Draft, ready to release' : 'No draft yet'; V.statusRows = [ { store: 'App Store', detail: 'Version 3.2.0 · build 412', state: appleState, stateColor: st.appleReleased ? 'var(--yellow)' : 'var(--text)', dotColor: st.appleReleased ? 'var(--yellow)' : 'var(--text3)', dotRadius: st.appleReleased ? '1px' : '50%' }, { store: 'Google Play', detail: 'Version 3.2.0 · version code 412 · production', state: googleState, stateColor: st.googleReleased ? 'var(--yellow)' : 'var(--text)', dotColor: st.googleReleased ? 'var(--yellow)' : 'var(--text3)', dotRadius: st.googleReleased ? '1px' : '50%' } ]; const googleBlocked = !st.rechecked && !ck.g1; V.appleBtnLabel = st.appleReleased ? 'Sent to App Store review' : 'Send to App Store review'; V.appleBtnBg = st.appleReleased ? 'var(--sep2)' : 'var(--redFill)'; V.appleBtnFg = st.appleReleased ? 'var(--text3)' : '#fff'; V.appleBtnBorder = st.appleReleased ? 'var(--sep)' : 'var(--redFill)'; V.appleBtnCursor = st.appleReleased ? 'default' : 'pointer'; V.appleHint = st.appleReleased ? 'Sent at 14:11. You can cancel this submission only before the review starts.' : 'This sends the App Store and nothing else. Google Play stays a draft.'; V.appleHintColor = 'var(--text2)'; V.googleBtnLabel = st.googleReleased ? 'Sent to Google Play review' : 'Send to Google Play review'; V.googleBtnBg = st.googleReleased ? 'var(--sep2)' : googleBlocked ? 'var(--sep2)' : 'var(--redFill)'; V.googleBtnFg = (st.googleReleased || googleBlocked) ? 'var(--text3)' : '#fff'; V.googleBtnBorder = (st.googleReleased || googleBlocked) ? 'var(--sep)' : 'var(--redFill)'; V.googleBtnCursor = (st.googleReleased || googleBlocked) ? 'not-allowed' : 'pointer'; V.googleHint = st.googleReleased ? 'Sent at 14:12. You can halt a staged rollout only. A completed rollout cannot be halted.' : googleBlocked ? 'Blocked: the content rating (IARC) is not done. Finish it in the Play Console, then press Re-check.' : 'This sends Google Play and nothing else. The App Store is untouched.'; V.googleHintColor = googleBlocked && !st.googleReleased ? 'var(--yellow)' : 'var(--text2)'; V.releaseApple = () => { if (!st.appleReleased) this.set('sheet', 'apple'); }; V.releaseGoogle = () => { if (!st.googleReleased && !googleBlocked) this.set('sheet', 'google'); }; V.sheetOpen = !!st.sheet; const isApple = st.sheet === 'apple'; V.sheetTitle = isApple ? 'Send Fast Bill Split to App Store review?' : 'Send Fast Bill Split to Google Play review?'; V.sheetRows = isApple ? [ { k: 'Store', v: 'App Store', font: 'inherit' }, { k: 'Version', v: '3.2.0', font: mono }, { k: 'Build', v: '412', font: mono }, { k: 'Release', v: 'After approval, phased over 7 days', font: 'inherit' } ] : [ { k: 'Store', v: 'Google Play', font: 'inherit' }, { k: 'Version', v: '3.2.0', font: mono }, { k: 'Version code', v: '412', font: mono }, { k: 'Release', v: 'Production track, completed rollout', font: 'inherit' } ]; V.sheetBody = isApple ? 'This sends the App Store version to Apple. It takes a place in the review queue. Google Play stays a draft.' : 'This commits the Google Play release to the production track. The App Store is untouched.'; V.sheetRecovery = isApple ? 'You can cancel this submission only before the review starts. Once a reviewer opens it, you cannot.' : 'You can halt a staged rollout. This release is a completed rollout, so it cannot be halted.'; V.sheetConfirm = isApple ? 'Send to App Store' : 'Send to Google Play'; V.closeSheet = () => this.set('sheet', null); V.confirmSheet = () => this.setState(isApple ? { appleReleased: true, sheet: null } : { googleReleased: true, sheet: null }); return V; } }
