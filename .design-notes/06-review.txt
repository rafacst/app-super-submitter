<div>  { padding:20px 28px 40px; flex-direction:column; gap:20px; max-width:940px }
  <div>  { flex-direction:column; gap:10px }
    <div>  { align-items:baseline; gap:9px }
      <div>  { font-size:12.5px; font-weight:600 }
        · Open rows
      <div>  { font-size:11.5px; color:var(--text2) }
        · 2 of 9 rows need you. They sit first, because this is the tab a developer forgets.
    <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--raised); overflow:hidden }
      <sc-for>
        <div>  { align-items:center; gap:12px; padding:11px 15px }
          <div>  { min-width:0; flex-direction:column; gap:2px }
            <div>  { font-size:12.5px; font-weight:500 }
              · {{ r.title }}
            <div>  { font-size:11.5px; color:var(--text2); line-height:1.45 }
              · {{ r.reason }}
          <div>  { padding:2px 7px; border-radius:5px; background:{{ r.bg }}; color:{{ r.fg }} }
            · {{ r.state }}
          <button>  { font-size:11.5px; padding:4px 11px; border-radius:6px; border:.5px solid var(--sep); background:var(--field); color:var(--text) }
            · {{ r.action }}
  <div>  { flex-direction:column; gap:10px }
    <div>  { font-size:12.5px; font-weight:600 }
      · Review contact
    <div>  { gap:12px }
      <div>  { min-width:150px; flex-direction:column; gap:4px }
        <div>  { font-size:11px; color:var(--text2) }
          · First name
        <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:6px 9px; font-size:12.5px }
          · Rafa
      <div>  { min-width:150px; flex-direction:column; gap:4px }
        <div>  { font-size:11px; color:var(--text2) }
          · Last name
        <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:6px 9px; font-size:12.5px }
          · C
      <div>  { min-width:190px; flex-direction:column; gap:4px }
        <div>  { font-size:11px; color:var(--text2) }
          · Email
        <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:6px 9px }
          · dev@fastbillsplit.app
      <div>  { min-width:150px; flex-direction:column; gap:4px }
        <div>  { font-size:11px; color:var(--text2) }
          · Phone
        <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:6px 9px }
          · +351 000 000 000
    <div>  { font-size:11.5px; color:var(--text2) }
      · App Store only. Google Play has no equivalent field.
  <div>  { flex-direction:column; gap:10px }
    <div>  { font-size:12.5px; font-weight:600 }
      · Demo account
    <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--raised); padding:13px 15px; flex-direction:column; gap:11px }
      <div>  { align-items:center; gap:9px }
        <div>  { width:34px; height:20px; border-radius:10px; background:var(--accent); padding:2px; justify-content:flex-end }
          <div>  { width:16px; height:16px; border-radius:50%; background:#fff }
        <div>  { font-size:12.5px }
          · The reviewer needs an account to sign in
      <div>  { gap:12px }
        <div>  { flex-direction:column; gap:4px }
          <div>  { font-size:11px; color:var(--text2) }
            · User name
          <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:6px 9px }
            · review@fastbillsplit.app
        <div>  { flex-direction:column; gap:4px }
          <div>  { font-size:11px; color:var(--text2) }
            · Password
          <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:6px 9px }
            · ••••••••••••
      <div>  { font-size:11.5px; color:var(--text2); line-height:1.5 }
        · The user name and the password go to the macOS Keychain. They are never written to a file in your repository.
  <div>  { flex-direction:column; gap:10px }
    <div>  { font-size:12.5px; font-weight:600 }
      · Everything else
    <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--raised); overflow:hidden }
      <sc-for>
        <div>  { align-items:center; gap:12px; padding:10px 15px }
          <div>  { min-width:0; flex-direction:column; gap:2px }
            <div>  { font-size:12.5px }
              · {{ r.title }}
            <div>  { font-size:11.5px; color:var(--text2) }
              · {{ r.reason }}
          <div>  { padding:2px 7px; border-radius:5px; background:{{ r.bg }}; color:{{ r.fg }} }
            · {{ r.state }}
          <button>  { font-size:11.5px; padding:4px 11px; border-radius:6px; border:.5px solid var(--sep); background:var(--field); color:var(--text); min-width:78px }
            · {{ r.action }}
