<div>  { position:absolute; background:var(--content); flex-direction:column }
  <div>  { height:52px; align-items:center; justify-content:space-between; padding:0 22px 0 20px }
    <div>  { align-items:center; gap:9px }
      <div>  { width:9px; height:9px; border-radius:50%; background:var(--accent) }
      <div>  { font-size:12.5px; font-weight:600; letter-spacing:-.005em }
        · Super Submitter
    <span>  { font-size:12px; color:var(--text2) | on={{ startApp }} }
      · Skip
  <div>  { align-items:center; justify-content:center; padding:0 48px }
    <sc-if>
      <div>  { gap:52px; align-items:center; max-width:900px; width:100% }
        <div>  { min-width:0; flex-direction:column; gap:16px }
          <div>  { align-items:center; gap:10px }
            <div>  { color:var(--accent); letter-spacing:.06em }
              · STEP {{ obNumber }} OF 5
            <div>  { height:1px; background:var(--sep2) }
          <div>  { font-size:23px; font-weight:600; letter-spacing:-.02em; line-height:1.25 }
            · {{ obTitle }}
          <div>  { flex-direction:column; gap:10px }
            <sc-for>
              <div>  { gap:10px; align-items:flex-start }
                <div>  { width:4px; height:4px; border-radius:50%; background:var(--text3) }
                <div>  { font-size:13px; line-height:1.6; color:var(--text2) }
                  · {{ p.t }}
        <div>  { width:330px }
          <sc-if>
            <div>  { flex-direction:column; gap:9px }
              <div>  { border:1.5px solid var(--accent); border-radius:9px; background:var(--raised); padding:11px 13px; align-items:center; justify-content:space-between }
                <div>  { font-size:12.5px; font-weight:600 }
                  · App Store
                <div>  { width:15px; height:15px; border-radius:50%; background:var(--accent); color:#fff; font-size:9px; align-items:center; justify-content:center }
                  · ✓
              <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--sunken); padding:11px 13px; flex-direction:column; gap:7px }
                <div>  { align-items:center; gap:9px }
                  <div>  { width:22px; height:27px; border-radius:3px; border:.5px solid var(--sep); background:var(--field) }
                  <div>  { color:var(--text2) }
                    · AuthKey_9F2KQ4X8L1.p8
                <div>  { gap:7px }
                  <div>  { border:.5px solid var(--sep); border-radius:5px; background:var(--field); padding:4px 7px; color:var(--text2) }
                    · key id
                  <div>  { border:.5px solid var(--sep); border-radius:5px; background:var(--field); padding:4px 7px; color:var(--text2) }
                    · issuer id
              <div>  { border:1.5px solid var(--accent); border-radius:9px; background:var(--raised); padding:11px 13px; align-items:center; justify-content:space-between }
                <div>  { font-size:12.5px; font-weight:600 }
                  · Google Play
                <div>  { width:15px; height:15px; border-radius:50%; background:var(--accent); color:#fff; font-size:9px; align-items:center; justify-content:center }
                  · ✓
              <div>  { align-items:center; gap:8px; padding:2px 2px 0 }
                <div>  { width:6px; height:6px; border-radius:2px; background:var(--green) }
                <div>  { font-size:11px; color:var(--text2) }
                  · Both keys go to the macOS Keychain.
          <sc-if>
            <div>  { border:.5px solid var(--sep); border-radius:10px; background:var(--raised); overflow:hidden }
              <div>  { padding:11px 13px; align-items:center; gap:10px }
                <div>  { width:32px; height:32px; border-radius:8px; background:repeating-linear-gradient(135deg,var(--sunken),var(--sunken) 5px,var(--sep2) 5px,var(--sep2) 10px); border:.5px solid var(--sep) }
                <div>
                  <div>  { font-size:12px; font-weight:600 }
                    · FastBillSplit.ipa
                  <div>  { color:var(--text2) }
                    · 118.4 MB · read in 1.2s
              <div>  { padding:5px 0 }
                <sc-for>
                  <div>  { gap:10px; padding:4px 13px; font-size:11.5px }
                    <div>  { width:96px; color:var(--text2) }
                      · {{ r.k }}
                    <div>  { font-family:var(--mono); font-size:11px }
                      · {{ r.v }}
              <div>  { padding:9px 13px; font-size:11.5px; color:var(--green) }
                · 8 fields filled on the Details tab.
          <sc-if>
            <div>  { border:.5px solid var(--sep); border-radius:10px; background:var(--raised); padding:13px; flex-direction:column; gap:13px }
              <div>  { flex-direction:column; gap:5px }
                <div>  { justify-content:space-between; align-items:baseline }
                  <div>  { font-size:11px; font-weight:500 }
                    · Subtitle
                  <div>  { color:var(--text2) }
                    · 26 / 30
                <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:6px 8px; font-size:12px }
                  · Split any bill in seconds
                <div>  { font-size:10.5px; color:var(--accent) }
                  · Different for Google
              <div>  { flex-direction:column; gap:5px }
                <div>  { justify-content:space-between; align-items:baseline }
                  <div>  { font-size:11px; font-weight:500 }
                    · Keywords
                  <div>  { color:var(--red) }
                    · 104 / 100
                <div>  { border:1px solid var(--red); border-radius:6px; background:var(--field); padding:6px 8px; line-height:1.4 }
                  · bill,split,tip,receipt,restaurant,dinner,share,check,tab,friends,group,payment
                <div>  { font-size:10.5px; color:var(--red) }
                  · Over the limit. We never shorten it for you.
          <sc-if>
            <div>  { border:.5px solid var(--sep); border-radius:10px; background:var(--raised); padding:13px; flex-direction:column; gap:11px }
              <div>  { justify-content:space-between; align-items:baseline }
                <div>  { font-size:11.5px; font-weight:600 }
                  · Phone
                <div>  { color:var(--text2) }
                  · 3 of 10
              <div>  { gap:8px }
                <div>  { flex-direction:column; gap:4px }
                  <div>  { height:104px; border-radius:5px; border:.5px solid var(--sep); background:repeating-linear-gradient(135deg,var(--sunken),var(--sunken) 6px,var(--sep2) 6px,var(--sep2) 12px) }
                  <div>  { color:var(--text2); line-height:1.3 }
                    · iPhone 6.7″
                <div>  { flex-direction:column; gap:4px }
                  <div>  { height:104px; border-radius:5px; border:.5px solid var(--sep); background:repeating-linear-gradient(135deg,var(--sunken),var(--sunken) 6px,var(--sep2) 6px,var(--sep2) 12px) }
                  <div>  { color:var(--text2); line-height:1.3 }
                    · iPhone 6.7″
                <div>  { flex-direction:column; gap:4px }
                  <div>  { height:104px; border-radius:5px; border:1px solid var(--red); background:var(--redBg) }
                  <div>  { color:var(--red); line-height:1.3 }
                    · 1179 × 2555
                    <br>
                    · no bucket
              <div>  { font-size:11px; color:var(--text2); line-height:1.5 }
                · Apple takes a video file. Google takes a YouTube link and no file.
          <sc-if>
            <div>  { flex-direction:column; gap:9px }
              <div>  { border:.5px solid var(--sep); border-radius:10px; background:var(--raised); padding:12px 13px; flex-direction:column; gap:8px }
                <div>  { justify-content:space-between; font-size:11.5px }
                  <span>  { color:var(--text2) }
                    · You asked for
                  <span>  { font-family:var(--mono) }
                    · 4.99 USD
                <div>  { justify-content:space-between; font-size:11.5px }
                  <span>  { color:var(--text2) }
                    · Apple resolved
                  <span>  { font-family:var(--mono); color:var(--green) }
                    · 4.99 USD
                <div>  { justify-content:space-between; font-size:11.5px }
                  <span>  { color:var(--text2) }
                    · Google base region
                  <span>  { font-family:var(--mono); color:var(--green) }
                    · 4.99 USD
              <div>  { gap:7px }
                <div>  { border:.5px solid var(--sep); border-radius:8px; background:var(--raised); padding:9px; font-size:11px; color:var(--text2) }
                  · None
                <div>  { border:1.5px solid var(--accent); border-radius:8px; background:var(--raised); padding:9px; font-size:11px; font-weight:600 }
                  · RevenueCat
                <div>  { border:.5px solid var(--sep); border-radius:8px; background:var(--raised); padding:9px; font-size:11px; color:var(--text2) }
                  · Adapty
              <div>  { font-size:11px; color:var(--text2); line-height:1.5; padding:0 2px }
                · One product id becomes the right object in each store, and one mirror in your provider.
    <sc-if>
      <div>  { flex-direction:column; align-items:center; gap:30px; max-width:640px }
        <div>  { font-size:34px; font-weight:600; letter-spacing:-.03em; line-height:1.2 }
          · We prepare a draft.
          <br>
          · You press release.
        <div>  { gap:0; align-items:stretch; width:100%; border:.5px solid var(--sep); border-radius:10px; background:var(--raised); overflow:hidden }
          <div>  { padding:13px 15px; flex-direction:column; gap:4px }
            <div>  { color:var(--text3); letter-spacing:.06em }
              · TABS 1–6
            <div>  { font-size:12px; line-height:1.5 }
              · You fill the forms. Nothing leaves this Mac.
          <div>  { width:.5px; background:var(--sep) }
          <div>  { padding:13px 15px; flex-direction:column; gap:4px }
            <div>  { color:var(--text3); letter-spacing:.06em }
              · TABS 7–8
            <div>  { font-size:12px; line-height:1.5 }
              · You read the diff. We write a draft to each store.
          <div>  { width:.5px; background:var(--sep) }
          <div>  { padding:13px 15px; flex-direction:column; gap:4px }
            <div>  { color:var(--red); letter-spacing:.06em }
              · TAB 9
            <div>  { font-size:12px; line-height:1.5 }
              · Two red buttons, one per store. The only irreversible step.
        <div>  { font-size:12.5px; color:var(--text2); line-height:1.6 }
          · Nothing before tab 9 can reach a customer, take a place in a review queue, or be undone by hand.
  <div>  { height:76px; align-items:center; justify-content:space-between; padding:0 28px }
    <div>  { width:120px }
      <sc-if>
        <button>  { font-size:12.5px; padding:7px 15px; border-radius:7px; border:.5px solid var(--sep); background:var(--field); color:var(--text) | on={{ obBack }} }
          · Back
    <div>  { gap:7px; align-items:center }
      <sc-for>
        <div>  { width:{{ d.w }}px; height:6px; border-radius:3px; background:{{ d.bg }} | on={{ d.go }} }
    <div>  { width:120px; justify-content:flex-end }
      <button>  { font-size:12.5px; font-weight:600; padding:7px 20px; border-radius:7px; border:none; background:var(--accent); color:var(--accentText) | on={{ obNext }} }
        · {{ obNextLabel }}
<script>
