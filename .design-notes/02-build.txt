<div>  { padding:22px 28px 40px; flex-direction:column; gap:20px; max-width:980px }
  <div>  { gap:14px; align-items:stretch }
    <div>  { border:.5px solid var(--sep); border-radius:10px; background:var(--raised); padding:15px 16px; flex-direction:column; gap:11px }
      <div>  { font-weight:600; font-size:13px }
        · Submit a build
      <div>  { flex-direction:column; gap:9px }
        <div>  { border:1px dashed var(--sep); border-radius:8px; background:var(--sunken); padding:12px 13px; align-items:center; gap:11px }
          <div>  { width:26px; height:32px; border-radius:4px; border:.5px solid var(--sep); background:var(--field) }
          <div>  { min-width:0 }
            <div>  { font-size:12px; font-weight:500 }
              · {{ iosFileLine }}
            <div>  { font-size:11px; color:var(--text2) }
              · .ipa or .pkg · drop here or
              <span>  { color:var(--accent) }
                · choose a file…
        <div>  { border:1px dashed var(--sep); border-radius:8px; background:var(--sunken); padding:12px 13px; align-items:center; gap:11px }
          <div>  { width:26px; height:32px; border-radius:4px; border:.5px solid var(--sep); background:var(--field) }
          <div>  { min-width:0 }
            <div>  { font-size:12px; font-weight:500 }
              · {{ aabFileLine }}
            <div>  { font-size:11px; color:var(--text2) }
              · .aab · drop here or
              <span>  { color:var(--accent) }
                · choose a file…
      <sc-if>
        <button>  { font-size:12px; padding:6px 13px; border-radius:6px; border:none; background:var(--accent); color:var(--accentText); font-weight:500 | on={{ readBuild }} }
          · Simulate a drop of both packages
    <div>  { width:1px; background:var(--sep2) }
    <div>  { border:.5px solid var(--sep); border-radius:10px; background:var(--raised); padding:15px 16px; flex-direction:column; gap:11px }
      <div>  { font-weight:600; font-size:13px }
        · Update an app that exists
      <div>  { flex-direction:column; gap:9px }
        <div>  { flex-direction:column; gap:4px }
          <div>  { font-size:11px; color:var(--text2) }
            · App Store
          <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:6px 9px; font-size:12px; justify-content:space-between; align-items:center }
            <span>
              · Fast Bill Split
              <span>  { color:var(--text2) }
                · 1234567890
            <span>  { color:var(--text3); font-size:9px }
              · ▾
        <div>  { flex-direction:column; gap:4px }
          <div>  { font-size:11px; color:var(--text2) }
            · Google Play
          <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:6px 9px; font-size:12px; justify-content:space-between; align-items:center }
            <span>
              · com.fastbillsplit.app
            <span>  { color:var(--text3); font-size:9px }
              · ▾
      <div>  { font-size:11.5px; color:var(--text2); line-height:1.5 }
        · Both paths end in the same state. Picking an app reads its current listing into the forms.
  <sc-if>
    <div>  { flex-direction:column; gap:14px }
      <div>  { gap:14px; align-items:flex-start }
        <sc-for>
          <div>  { border:.5px solid var(--sep); border-radius:10px; background:var(--raised); overflow:hidden }
            <div>  { padding:12px 15px; align-items:center; gap:11px }
              <div>  { width:38px; height:38px; border-radius:9px; background:repeating-linear-gradient(135deg,var(--sunken),var(--sunken) 5px,var(--sep2) 5px,var(--sep2) 10px); border:.5px solid var(--sep) }
              <div>  { min-width:0 }
                <div>  { font-weight:600; font-size:13px }
                  · {{ pkg.title }}
                <div>  { color:var(--text2); overflow:hidden }
                  · {{ pkg.file }}
            <div>  { padding:4px 0 }
              <sc-for>
                <div>  { gap:12px; padding:5px 15px; font-size:12px; align-items:baseline }
                  <div>  { width:112px; color:var(--text2) }
                    · {{ r.k }}
                  <div>  { font-family:{{ r.font }}; min-width:0 }
                    · {{ r.v }}
      <div>  { align-items:center; gap:14px; border:.5px solid var(--sep); border-radius:9px; background:var(--sunken); padding:11px 15px }
        <div>  { font-size:12.5px }
          · We filled 8 fields on the Details tab.
        <button>  { font-size:12px; padding:5px 12px; border-radius:6px; border:.5px solid var(--sep); background:var(--field); color:var(--text) | on={{ goTab3 }} }
          · Open Details
      <div>  { gap:10px; align-items:flex-start; border:1px solid var(--yellow); background:var(--yellowBg); border-radius:8px; padding:11px 14px }
        <span>  { font-weight:700; color:var(--yellow); line-height:1.4 }
          · !
        <div>  { flex-direction:column; gap:3px }
          <div>  { font-size:12.5px; font-weight:500 }
            · The version name differs between the two packages.
          <div>  { font-size:12px; color:var(--text2); line-height:1.5 }
            · The .ipa reads 3.2.0 and the .aab reads 3.2.0-rc4. Google Play shows the release name to nobody, but the two stores then hold two different numbers. Set one name on the Build tab.
        <button>  { font-size:12px; padding:4px 11px; border-radius:6px; border:.5px solid var(--sep); background:var(--field); color:var(--text) }
          · Use 3.2.0
