<div>  { padding:20px 28px 40px; flex-direction:column; gap:22px }
  <sc-for>
    <div>  { flex-direction:column; gap:10px }
      <div>  { align-items:baseline; gap:10px }
        <div>  { font-size:12.5px; font-weight:600 }
          · {{ g.name }}
        <div>  { color:var(--text2) }
          · {{ g.count }}
        <div>  { font-size:11px; color:var(--text3) }
          · {{ g.note }}
      <div>  { gap:10px }
        <sc-for>
          <div>  { width:{{ s.w }}px; flex-direction:column; gap:5px }
            <div>  { width:{{ s.w }}px; height:{{ s.h }}px; border-radius:6px; border:.5px solid var(--sep); background:repeating-linear-gradient(135deg,var(--sunken),var(--sunken) 6px,var(--sep2) 6px,var(--sep2) 12px) }
            <div>  { color:var(--text2); line-height:1.35 }
              · {{ s.bucket }}
            <div>  { color:var(--text3) }
              · {{ s.stores }}
        <div>  { width:{{ g.dropW }}px; height:{{ g.dropH }}px; border-radius:6px; border:1px dashed var(--sep); align-items:center; justify-content:center; font-size:11px; color:var(--text3); padding:8px; line-height:1.4 }
          · Drop images
          <br>
          · or choose files
  <div>  { flex-direction:column; gap:11px }
    <div>  { font-size:12.5px; font-weight:600 }
      · Video
    <div>  { font-size:12px; color:var(--text2) }
      · Apple takes a video file. Google takes a YouTube link and no file.
    <div>  { gap:14px; align-items:stretch; max-width:820px }
      <div>  { border:1px dashed var(--sep); border-radius:9px; background:var(--sunken); padding:14px; flex-direction:column; gap:6px }
        <div>  { font-size:11.5px; font-weight:600 }
          · App Store · app preview
        <div>  { font-size:11.5px; color:var(--text2); line-height:1.5 }
          · Drop a .mov, .m4v, or .mp4. 15 to 30 seconds. At most 3 per device size.
        <div>  { align-items:center; gap:9px; background:var(--field); border:.5px solid var(--sep); border-radius:6px; padding:7px 9px }
          <div>  { width:38px; height:24px; border-radius:3px; background:repeating-linear-gradient(135deg,var(--sunken),var(--sunken) 5px,var(--sep2) 5px,var(--sep2) 10px) }
          <div>  { min-width:0; overflow:hidden }
            · preview-6.7-en.mov
          <div>  { color:var(--text2) }
            · 22s
      <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--raised); padding:14px; flex-direction:column; gap:6px }
        <div>  { font-size:11.5px; font-weight:600 }
          · Google Play · YouTube URL
        <div>  { font-size:11.5px; color:var(--text2); line-height:1.5 }
          · Google accepts no video file. A link, or nothing.
        <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:7px 9px }
          · https://youtube.com/watch?v=7Qm2xKp
