<div>  { padding:20px 28px 40px; flex-direction:column; gap:22px; max-width:940px }
  <div>  { flex-direction:column; gap:11px }
    <div>  { font-size:11px; font-weight:600; letter-spacing:.05em; text-transform:uppercase; color:var(--text3) }
      · Subscription provider
    <div>  { gap:10px }
      <sc-for>
        <div>  { border:{{ p.border }}; border-radius:9px; background:var(--raised); padding:12px 14px; flex-direction:column; gap:4px | on={{ p.select }} }
          <div>  { align-items:center; justify-content:space-between }
            <div>  { font-size:13px; font-weight:600 }
              · {{ p.name }}
            <div>  { width:15px; height:15px; border-radius:50%; background:{{ p.dot }}; color:#fff; font-size:9px; align-items:center; justify-content:center }
              · {{ p.mark }}
          <div>  { font-size:11.5px; color:var(--text2); line-height:1.45 }
            · {{ p.line }}
    <sc-if>
      <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--sunken); padding:14px 15px; flex-direction:column; gap:12px }
        <div>  { gap:14px }
          <div>  { flex-direction:column; gap:4px }
            <div>  { font-size:11px; color:var(--text2) }
              · Secret v2 API key
            <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:6px 9px }
              · sk_••••••••••••••••••7Qk2
          <div>  { flex-direction:column; gap:4px }
            <div>  { font-size:11px; color:var(--text2) }
              · Project
            <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:6px 9px; font-size:12px; justify-content:space-between }
              <span>
                · Fast Bill Split
              <span>  { color:var(--text3); font-size:9px }
                · ▾
        <div>  { gap:6px }
          <sc-for>
            <div>  { border:.5px solid var(--sep); border-radius:4px; padding:2px 6px; color:var(--text2) }
              · {{ s.k }}
        <div>  { align-items:center; gap:12px }
          <div>  { font-size:11.5px; color:var(--green) }
            · ● Connected. 5 of 5 scopes present.
          <span>  { font-size:11.5px; color:var(--accent) }
            · I have no account yet ↗
          <span>  { font-size:11.5px; color:var(--text2) }
            · The key goes to the macOS Keychain.
    <sc-if>
      <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--sunken); padding:14px 15px; flex-direction:column; gap:10px }
        <div>  { font-size:12px; color:var(--text2); line-height:1.5 }
          · Adapty holds no key that this app can store. The adapty command line tool owns its own login.
        <div>  { align-items:center; gap:9px; font-size:12px }
          <span>  { color:var(--green) }
            · ●
          · Logged in as
          <span>  { font-family:var(--mono) }
            · rafa@fastbillsplit.app
        <div>  { align-items:center; gap:8px; background:var(--field); border:.5px solid var(--sep); border-radius:6px; padding:7px 9px; max-width:420px }
          <span>
            · adapty auth login
          <button>  { font-size:11px; padding:3px 9px; border-radius:5px; border:.5px solid var(--sep); background:var(--raised); color:var(--text) }
            · Copy
        <div>  { font-size:11.5px; color:var(--text2) }
          · This app never runs the login command. A login opens a browser and it belongs to you.
  <div>  { flex-direction:column; gap:11px }
    <div>  { font-size:11px; font-weight:600; letter-spacing:.05em; text-transform:uppercase; color:var(--text3) }
      · Price
    <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--raised); padding:13px 15px; gap:26px; align-items:flex-end }
      <div>  { flex-direction:column; gap:4px }
        <div>  { font-size:11px; color:var(--text2) }
          · Amount
        <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:6px 9px; width:86px }
          · 4.99
      <div>  { flex-direction:column; gap:4px }
        <div>  { font-size:11px; color:var(--text2) }
          · Currency
        <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:6px 9px; width:74px }
          · USD
      <div>  { flex-direction:column; gap:4px }
        <div>  { font-size:11px; color:var(--text2) }
          · Base country
        <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:6px 9px; font-size:12px; width:150px; justify-content:space-between }
          <span>
            · United States
          <span>  { color:var(--text3); font-size:9px }
            · ▾
      <div>  { height:32px; width:1px; background:var(--sep2) }
      <div>  { flex-direction:column; gap:3px }
        <div>  { font-size:11px; color:var(--text2) }
          · Apple resolved this price point
        <div>  { color:var(--green) }
          · USD 4.99 · exact match
      <div>  { flex-direction:column; gap:3px }
        <div>  { font-size:11px; color:var(--text2) }
          · Google base region price
        <div>  { color:var(--green) }
          · USD 4.99
  <div>  { flex-direction:column; gap:11px }
    <div>  { font-size:11px; font-weight:600; letter-spacing:.05em; text-transform:uppercase; color:var(--text3) }
      · Availability
    <div>  { gap:14px }
      <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--raised); padding:13px 15px; flex-direction:column; gap:6px }
        <div>  { font-size:12px; font-weight:600 }
          · App Store · 175 countries
        <div>  { font-size:11.5px; color:var(--text2); line-height:1.5 }
          · This app writes the territory list.
        <button>  { font-size:11.5px; padding:4px 11px; border-radius:6px; border:.5px solid var(--sep); background:var(--field); color:var(--text) }
          · Edit countries
      <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--raised); padding:13px 15px; flex-direction:column; gap:6px }
        <div>  { font-size:12px; font-weight:600 }
          · Google Play · 171 countries
        <div>  { font-size:11.5px; color:var(--text2); line-height:1.5 }
          · Read-only. The Android Publisher API does not write the country list.
        <button>  { font-size:11.5px; padding:4px 11px; border-radius:6px; border:.5px solid var(--sep); background:var(--field); color:var(--text) }
          · Open Play Console ↗
  <div>  { flex-direction:column; gap:11px }
    <div>  { font-size:11px; font-weight:600; letter-spacing:.05em; text-transform:uppercase; color:var(--text3) }
      · In-app purchases
    <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--raised); overflow:hidden }
      <div>  { gap:12px; padding:7px 15px; font-size:10.5px; color:var(--text3); letter-spacing:.03em }
        <div>
          · Product id
        <div>  { width:120px }
          · Kind
        <div>  { width:170px }
          · Name, en-US
        <div>  { width:70px }
          · Price
      <div>  { gap:12px; padding:9px 15px; font-size:12px; align-items:center }
        <div>  { font-family:var(--mono); font-size:11.5px }
          · com.fastbillsplit.app.pro
        <div>  { width:120px; color:var(--text2) }
          · Non-consumable
        <div>  { width:170px }
          · Pro Unlock
        <div>  { width:70px; font-family:var(--mono) }
          · 9.99
  <div>  { flex-direction:column; gap:11px }
    <div>  { font-size:11px; font-weight:600; letter-spacing:.05em; text-transform:uppercase; color:var(--text3) }
      · Subscriptions
    <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--raised); overflow:hidden }
      <div>  { padding:10px 15px; align-items:baseline; gap:9px }
        <div>  { font-size:12.5px; font-weight:600 }
          · Fast Bill Split Premium
        <div>  { color:var(--text2) }
          · group: main
      <sc-for>
        <div>  { gap:12px; padding:9px 15px; font-size:12px; align-items:center }
          <div>  { font-family:var(--mono); font-size:11.5px }
            · {{ p.id }}
          <div>  { width:90px; color:var(--text2) }
            · {{ p.duration }}
          <div>  { width:110px; font-family:var(--mono); font-size:11.5px; color:var(--text2) }
            · {{ p.basePlan }}
          <div>  { width:150px }
            · {{ p.name }}
          <div>  { width:70px; font-family:var(--mono) }
            · {{ p.price }}
  <sc-if>
    <div>  { gap:14px }
      <div>  { flex-direction:column; gap:9px }
        <div>  { font-size:11px; font-weight:600; letter-spacing:.05em; text-transform:uppercase; color:var(--text3) }
          · {{ entitlementLabel }}
        <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--raised); overflow:hidden }
          <div>  { gap:12px; padding:8px 14px; font-size:12px }
            <div>  { font-family:var(--mono); font-size:11.5px }
              · pro
            <div>  { color:var(--text2) }
              · Pro
          <div>  { gap:12px; padding:8px 14px; font-size:12px }
            <div>  { font-family:var(--mono); font-size:11.5px }
              · premium
            <div>  { color:var(--text2) }
              · Premium
      <div>  { flex-direction:column; gap:9px }
        <div>  { font-size:11px; font-weight:600; letter-spacing:.05em; text-transform:uppercase; color:var(--text3) }
          · {{ offeringLabel }}
        <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--raised); overflow:hidden }
          <div>  { gap:12px; padding:8px 14px; font-size:12px; align-items:center }
            <div>  { font-family:var(--mono); font-size:11.5px }
              · default
            <div>  { color:var(--text2) }
              · Standard offering
            <div>  { border:.5px solid var(--sep); border-radius:4px; padding:1px 5px; color:var(--text2) }
              · current
          <div>  { padding:6px 14px 9px; font-size:11.5px; color:var(--text2); font-family:var(--mono) }
            · premium.monthly, premium.yearly
