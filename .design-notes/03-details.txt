<div>  { height:100% }
  <div>  { min-width:0; overflow:auto; padding:20px 24px 40px; flex-direction:column; gap:16px }
    <div>  { flex-direction:column; gap:5px }
      <div>  { align-items:baseline; justify-content:space-between; gap:10px }
        <div>  { align-items:center; gap:7px }
          <div>  { font-size:11.5px; font-weight:500 }
            · Name
          <div>  { color:var(--text3); border:.5px solid var(--sep); border-radius:4px; padding:0 4px }
            · from the build
        <div>  { color:var(--text2) }
          · 15 / 30
      <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:7px 9px; font-size:13px }
        · Fast Bill Split
    <div>  { flex-direction:column; gap:5px }
      <div>  { align-items:baseline; justify-content:space-between; gap:10px }
        <div>  { align-items:center; gap:7px }
          <div>  { font-size:11.5px; font-weight:500 }
            · Subtitle
          <div>  { color:var(--text3) }
            · Apple 30 · Google 80
        <div>  { align-items:center; gap:10px }
          <span>  { font-size:11px; color:var(--accent) }
            · Different for Google
          <span>  { color:var(--text2) }
            · 26 / 30
      <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:7px 9px; font-size:13px }
        · Split any bill in seconds
    <div>  { flex-direction:column; gap:5px }
      <div>  { align-items:baseline; justify-content:space-between; gap:10px }
        <div>  { font-size:11.5px; font-weight:500 }
          · Description
        <div>  { color:var(--text2) }
          · 318 / 4000
      <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:8px 9px; font-size:13px; line-height:1.55; height:96px; color:var(--text) }
        · Split a restaurant bill with your friends. No account. No ads. Scan the receipt, tap the items each person ordered, and Fast Bill Split works out the tip and the tax for every share.
    <div>  { flex-direction:column; gap:5px }
      <div>  { align-items:baseline; justify-content:space-between; gap:10px }
        <div>  { font-size:11.5px; font-weight:500 }
          · What is new
        <div>  { align-items:center; gap:10px }
          <span>  { font-size:11px; color:var(--text2) }
            · Google override on
          <span>  { color:var(--text2) }
            · 44 / 500
      <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:7px 9px; font-size:13px }
        · Faster scanning and a new dark theme.
      <div>  { align-items:center; gap:8px }
        <div>  { color:var(--text3); border:.5px solid var(--sep); border-radius:4px; padding:0 4px }
          · Google only
        <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:6px 9px; font-size:12.5px }
          · Faster scanning and a new dark theme.
        <div>  { color:var(--text2) }
          · 44 / 500
    <div>  { gap:14px }
      <div>  { flex-direction:column; gap:5px }
        <div>  { align-items:baseline; justify-content:space-between; gap:10px }
          <div>  { align-items:center; gap:7px }
            <div>  { font-size:11.5px; font-weight:500 }
              · Keywords
            <div>  { color:var(--text3); border:.5px solid var(--sep); border-radius:4px; padding:0 4px }
              · Apple only
          <div>  { color:{{ kwCountColor }}; font-weight:{{ kwCountWeight }} }
            · {{ kwCount }}
        <div>  { border:{{ kwBorder }}; border-radius:6px; background:var(--field); padding:7px 9px; line-height:1.45 }
          · {{ kwText }}
        <sc-if>
          <div>  { align-items:center; gap:10px }
            <div>  { font-size:11px; color:var(--red) }
              · 4 characters over the limit. Apple refuses the write. This app never shortens your text.
            <button>  { font-size:11px; padding:3px 9px; border-radius:5px; border:.5px solid var(--sep); background:var(--field); color:var(--text) | on={{ fixKeywords }} }
              · Drop the last two (demo)
      <div>  { flex-direction:column; gap:5px }
        <div>  { align-items:baseline; justify-content:space-between; gap:10px }
          <div>  { align-items:center; gap:7px }
            <div>  { font-size:11.5px; font-weight:500 }
              · Promotional text
            <div>  { color:var(--text3); border:.5px solid var(--sep); border-radius:4px; padding:0 4px }
              · Apple only
          <div>  { color:var(--text2) }
            · 28 / 170
        <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:7px 9px; font-size:13px }
          · Now with receipt scanning.
    <div>  { gap:14px }
      <div>  { flex-direction:column; gap:5px }
        <div>  { align-items:center; gap:7px }
          <div>  { font-size:11.5px; font-weight:500 }
            · Short description
          <div>  { color:var(--text3); border:.5px solid var(--sep); border-radius:4px; padding:0 4px }
            · Google only
        <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:7px 9px; font-size:13px }
          · Split any bill in seconds with your friends
      <div>  { flex-direction:column; gap:5px }
        <div>  { font-size:11.5px; font-weight:500 }
          · Support URL
        <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:7px 9px }
          · https://fastbillsplit.app/support
  <sc-if>
    <div>  { width:300px; background:var(--sunken); overflow:auto; padding:18px 18px 40px; flex-direction:column; gap:14px }
      <div>  { font-size:11px; font-weight:600; letter-spacing:.05em; text-transform:uppercase; color:var(--text3) }
        · What each store receives
      <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--content); overflow:hidden }
        <div>  { padding:8px 12px; font-size:11.5px; font-weight:600 }
          · App Store · en-US
        <div>  { padding:11px 12px; flex-direction:column; gap:8px }
          <div>
            <div>  { font-size:15px; font-weight:600; letter-spacing:-.01em }
              · Fast Bill Split
            <div>  { font-size:12px; color:var(--text2) }
              · Split any bill in seconds
          <div>  { font-size:11.5px; line-height:1.5; color:var(--text2) }
            · Split a restaurant bill with your friends. No account. No ads. Scan the receipt, tap the items…
          <div>  { font-size:11px; color:{{ kwPreviewColor }} }
            · {{ kwPreviewLine }}
      <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--content); overflow:hidden }
        <div>  { padding:8px 12px; font-size:11.5px; font-weight:600 }
          · Google Play · en-US
        <div>  { padding:11px 12px; flex-direction:column; gap:8px }
          <div>
            <div>  { font-size:15px; font-weight:600; letter-spacing:-.01em }
              · Fast Bill Split
            <div>  { font-size:12px; color:var(--text2) }
              · Split any bill in seconds with your friends
          <div>  { font-size:11.5px; line-height:1.5; color:var(--text2) }
            · Split a restaurant bill with your friends. No account. No ads. Scan the receipt, tap the items…
          <div>  { font-size:11px; color:var(--text2) }
            · Release note: 44 of 500 characters.
