<div>  { padding:20px 28px 40px; flex-direction:column; gap:16px; max-width:860px }
  <sc-if>
    <div>  { flex-direction:column; gap:14px; align-items:flex-start }
      <div>  { font-size:15px; font-weight:600 }
        · Ready to run.
      <div>  { font-size:13px; color:var(--text2); line-height:1.6; max-width:520px }
        · 24 writes and 13 uploads, across App Store, Google Play, and RevenueCat. The run ends with a draft in each store. It sends nothing to review.
      <button>  { font-size:12.5px; padding:6px 14px; border-radius:7px; border:.5px solid var(--sep); background:var(--field); color:var(--text) | on={{ goTab7 }} }
        · Review the plan first
  <sc-if>
    <div>  { flex-direction:column; gap:16px }
      <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--raised); overflow:hidden }
        <sc-for>
          <div>  { align-items:center; gap:11px; padding:{{ s.pad }}; background:{{ s.rowBg }} }
            <div>  { width:16px; align-items:center; justify-content:center }
              · {{ s.mark }}
            <div>  { min-width:0; font-size:{{ s.size }}; font-weight:{{ s.weight }}; color:{{ s.color }} }
              · {{ s.text }}
            <div>  { color:var(--text2) }
              · {{ s.meta }}
      <sc-if>
        <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--sunken); padding:13px 15px; flex-direction:column; gap:9px }
          <div>  { align-items:baseline; justify-content:space-between }
            <div>  { font-size:12.5px; font-weight:500 }
              · Uploading FastBillSplit.ipa · 118.4 MB
            <div>  { color:var(--text2) }
              · {{ uploadClock }} elapsed · about 8 minutes total
          <div>  { height:6px; border-radius:3px; background:var(--sep); overflow:hidden }
            <div>  { height:6px; border-radius:3px; background:var(--accent); width:{{ uploadPct }}% }
          <div>  { align-items:center; justify-content:space-between }
            <div>  { font-size:11.5px; color:var(--text2) }
              · Apple processes the build after the upload. This is the longest wait in the app.
            <button>  { font-size:11.5px; padding:4px 12px; border-radius:6px; border:.5px solid var(--sep); background:var(--field); color:var(--text) }
              · Cancel
      <div>  { border:.5px solid var(--sep); border-radius:9px; background:var(--sunken); overflow:hidden }
        <div>  { padding:9px 14px; align-items:center; gap:8px; font-size:12px | on={{ toggleLog }} }
          <span>  { font-size:9px; color:var(--text2) }
            · {{ logArrow }}
          · Log
        <sc-if>
          <div>  { padding:10px 14px; color:var(--text2); overflow:auto }
            · {{ logText }}
      <sc-if>
        <div>  { flex-direction:column; gap:12px }
          <div>  { font-size:17px; font-weight:600; letter-spacing:-.01em }
            · The run ended. Every store holds a draft.
          <div>  { font-size:13px; color:var(--text2); line-height:1.6; max-width:560px }
            · Nothing went to review. Nothing reached a customer. Both drafts are visible in the two consoles.
          <button>  { font-size:12.5px; padding:7px 15px; border-radius:7px; border:none; background:var(--accent); color:var(--accentText); font-weight:500 | on={{ goTab9 }} }
            · Go to Release
