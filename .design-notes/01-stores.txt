<div>  { padding:22px 28px 40px; flex-direction:column; gap:22px; max-width:900px }
  <div>  { gap:14px }
    <div>  { border:1.5px solid var(--accent); border-radius:10px; padding:14px 16px; background:var(--raised); flex-direction:column; gap:6px }
      <div>  { align-items:center; justify-content:space-between }
        <div>  { font-weight:600; font-size:13.5px }
          · App Store
        <div>  { width:16px; height:16px; border-radius:50%; background:var(--accent); color:#fff; font-size:10px; align-items:center; justify-content:center }
          · ✓
      <div>  { font-size:11.5px; color:var(--text2); line-height:1.45 }
        · iOS and Mac App Store. Written through the App Store Connect API.
    <div>  { border:1.5px solid var(--accent); border-radius:10px; padding:14px 16px; background:var(--raised); flex-direction:column; gap:6px }
      <div>  { align-items:center; justify-content:space-between }
        <div>  { font-weight:600; font-size:13.5px }
          · Google Play
        <div>  { width:16px; height:16px; border-radius:50%; background:var(--accent); color:#fff; font-size:10px; align-items:center; justify-content:center }
          · ✓
      <div>  { font-size:11.5px; color:var(--text2); line-height:1.45 }
        · Android. Written through the Android Publisher API.
  <div>  { border:.5px solid var(--sep); border-radius:10px; background:var(--raised); overflow:hidden }
    <div>  { padding:13px 16px; align-items:center; justify-content:space-between }
      <div>  { font-weight:600; font-size:13px }
        · App Store credential
      <div>  { align-items:center; gap:6px; font-size:11.5px; color:var(--green) }
        <span>  { font-size:10px }
          · ●
        · Connected · Fast Bill Split Lda.
    <div>  { padding:14px 16px; flex-direction:column; gap:12px }
      <div>  { gap:12px; align-items:stretch }
        <div>  { border:1px dashed var(--sep); border-radius:8px; padding:12px; align-items:center; gap:10px; background:var(--sunken) }
          <div>  { width:30px; height:36px; border-radius:4px; border:.5px solid var(--sep); background:var(--field) }
          <div>  { flex-direction:column; gap:2px; min-width:0 }
            <div>  { overflow:hidden }
              · AuthKey_9F2KQ4X8L1.p8
            <div>  { font-size:11px; color:var(--text2) }
              · Drop the .p8 file, or
              <span>  { color:var(--accent) }
                · choose a file…
        <div>  { flex-direction:column; gap:8px }
          <div>  { flex-direction:column; gap:3px }
            <div>  { font-size:11px; color:var(--text2) }
              · Key id
            <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:5px 8px }
              · 9F2KQ4X8L1
          <div>  { flex-direction:column; gap:3px }
            <div>  { font-size:11px; color:var(--text2) }
              · Issuer id
            <div>  { border:.5px solid var(--sep); border-radius:6px; background:var(--field); padding:5px 8px }
              · 57246542-96fe-1a63-e053-0824d011072a
      <div>  { align-items:center; gap:6px; font-size:12px; color:var(--accent) | on={{ toggleAppleGuide }} }
        <span>  { font-size:9px }
          · {{ appleGuideArrow }}
        · Where do I get this?
      <sc-if>
        <div>  { border:.5px solid var(--sep); border-radius:8px; background:var(--sunken); padding:13px 15px; flex-direction:column; gap:9px }
          <div>  { gap:9px; font-size:12px; line-height:1.5 }
            <span>  { color:var(--text2) }
              · 1
            <span>
              · Open App Store Connect, then Users and Access, then Integrations, then App Store Connect API.
          <div>  { gap:9px; font-size:12px; line-height:1.5 }
            <span>  { color:var(--text2) }
              · 2
            <span>
              · Create a key with the App Manager role. Copy the key id and the issuer id.
          <div>  { gap:9px; font-size:12px; line-height:1.5 }
            <span>  { color:var(--text2) }
              · 3
            <span>
              · Download the .p8 file.
          <div>  { gap:9px; align-items:flex-start; border:1px solid var(--yellow); background:var(--yellowBg); border-radius:7px; padding:9px 11px; font-size:12px; line-height:1.5; color:var(--text) }
            <span>  { font-weight:700; color:var(--yellow) }
              · !
            <span>
              · Apple shows the .p8 file once. Save it now. A lost key cannot be downloaded again — you create a new key.
          <button>  { font-size:12px; padding:5px 12px; border-radius:6px; border:.5px solid var(--sep); background:var(--field); color:var(--text) }
            · Open Users and Access ↗
      <div>  { align-items:center; gap:12px }
        <button>  { font-size:12px; padding:5px 13px; border-radius:6px; border:.5px solid var(--sep); background:var(--field); color:var(--text) }
          · Test connection
        <div>  { font-size:11.5px; color:var(--text2) }
          · The key goes to the macOS Keychain. This app keeps no copy.
  <div>  { border:.5px solid var(--sep); border-radius:10px; background:var(--raised); overflow:hidden }
    <div>  { padding:13px 16px; align-items:center; justify-content:space-between }
      <div>  { font-weight:600; font-size:13px }
        · Google Play credential
      <div>  { align-items:center; gap:6px; font-size:11.5px; color:var(--green) }
        <span>  { font-size:10px }
          · ●
        · Connected · fastbillsplit-ci@…iam.gserviceaccount.com
    <div>  { padding:14px 16px; flex-direction:column; gap:12px }
      <div>  { border:1px dashed var(--sep); border-radius:8px; padding:12px; align-items:center; gap:10px; background:var(--sunken); max-width:420px }
        <div>  { width:30px; height:36px; border-radius:4px; border:.5px solid var(--sep); background:var(--field) }
        <div>  { flex-direction:column; gap:2px; min-width:0 }
          <div>  { overflow:hidden }
            · play-service-account.json
          <div>  { font-size:11px; color:var(--text2) }
            · Drop the service account JSON, or
            <span>  { color:var(--accent) }
              · choose a file…
      <div>  { align-items:center; gap:6px; font-size:12px; color:var(--accent) | on={{ toggleGoogleGuide }} }
        <span>  { font-size:9px }
          · {{ googleGuideArrow }}
        · Where do I get this?
      <sc-if>
        <div>  { border:.5px solid var(--sep); border-radius:8px; background:var(--sunken); padding:13px 15px; flex-direction:column; gap:9px }
          <div>  { gap:9px; font-size:12px; line-height:1.5 }
            <span>  { color:var(--text2) }
              · 1
            <span>
              · In the Google Cloud console, create a service account and download its JSON key.
          <div>  { gap:9px; font-size:12px; line-height:1.5 }
            <span>  { color:var(--text2) }
              · 2
            <span>
              · Grant it the Android Publisher role.
          <div>  { gap:9px; font-size:12px; line-height:1.5 }
            <span>  { color:var(--text2) }
              · 3
            <span>
              · In the Play Console, open Users and permissions and invite the service account email.
          <div>  { gap:9px; align-items:flex-start; border:1px solid var(--yellow); background:var(--yellowBg); border-radius:7px; padding:9px 11px; font-size:12px; line-height:1.5 }
            <span>  { font-weight:700; color:var(--yellow) }
              · !
            <span>
              · Step 3 is mandatory and no API performs it. A skipped invitation returns a permission error later, with no clue about the cause.
          <div>  { gap:8px }
            <button>  { font-size:12px; padding:5px 12px; border-radius:6px; border:.5px solid var(--sep); background:var(--field); color:var(--text) }
              · Open Cloud console ↗
            <button>  { font-size:12px; padding:5px 12px; border-radius:6px; border:.5px solid var(--sep); background:var(--field); color:var(--text) }
              · Open Play Console ↗
      <div>  { align-items:center; gap:12px }
        <button>  { font-size:12px; padding:5px 13px; border-radius:6px; border:.5px solid var(--sep); background:var(--field); color:var(--text) }
          · Test connection
        <div>  { font-size:11.5px; color:var(--text2) }
          · The JSON goes to the macOS Keychain. This app keeps no copy.
